@model List<DapperProject.Dtos.LocationDtos.ResultLocationDto>
@{
    ViewData["Title"] = "LocationList";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
    int count = 0;
}

<div class="row">
    <div class="col">
        <div class="card shadow">
            <div class="card-header border-0">
                <h3 class="mb-0">Lokasyon Listesi</h3>
            </div>
            <div class="table-responsive">
                <table class="table align-items-center table-flush">
                    <thead class="thead-light">
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Lokasyon  </th>
                            <th scope="col">Sil</th>
                            <th scope="col">Güncelle</th>

                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            count++;
                            <tr>
                                <td>
                                    @count
                                </td>

                                <td>
                                    @item.LocationName
                                </td>
                                <td><a href="/Admin/Location/DeleteLocation/@item.LocationId" class="btn btn-warning">Sil</a></td>
                                <td><a href="/Admin/Location/UpdateLocation/@item.LocationId" class="btn btn-info">Güncelle</a></td>
                            </tr>
                        }
                    </tbody>
                </table>

            </div>
        </div>
        <br />
        <td><a href="/Admin/Location/CreateLocation/" class="btn btn-primary">Yeni Lokasyon Girişi</a></td>
    </div>
</div>